---
title: "Glosary"
echo: false
warning: false
---

```{r}
library(reactable)
library(tidyverse)
```

```{r}
the_data <- readRDS('data/the_data.rds')

reactable(the_data,
  columns = list(
    Notes = colDef(cell = function(value, index) {
      url <- sprintf(
        "/class_functions/notes/%s.html",
        the_data$Notes[index], value
      )
      htmltools::tags$a(href = url, as.character(value))
    },
    filterable = TRUE)
  )
)
```
