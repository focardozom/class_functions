# Function name: pivot_wider
# Notes:
# .blue[`pivot_wider()`] is a thing. 
+ There is function called `pivot_wider()` that will take long data and make it wider.
+ I rarely use it.  Know it exists.
---